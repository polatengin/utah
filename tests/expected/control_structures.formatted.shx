// Malformatted control structures
for(let i: number = 0; i < 10; i++) {
  if(i % 2 === 0) {
    console.log(`Even: ${i}`);
  } else {
    console.log(`Odd: ${i}`);
  }
}

while(condition) {
  let x = getValue();
  if(x > 10) {
    break;
  }
  console.log(x);
}

switch(value) {
  default:
    console.log("default");
}
