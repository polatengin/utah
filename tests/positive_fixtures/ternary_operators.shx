const x: number = 10;
const result: number = x > 5 ? x * 2 : x / 2;
console.log("Result: " + result);

let status: string = x >= 10 ? "high" : "low";
console.log("Status: " + status);

const value: number = 75;
let test: string = value > 80 ? "crit" : value > 60 ? "warn" : "ok";

alertLevel = value > 95 ? "CRITICAL" : "WARNING";
console.log(`⚠️  HIGH MEMORY USAGE: ${value}% (threshold: ${value}%) - ${alertLevel}`);
